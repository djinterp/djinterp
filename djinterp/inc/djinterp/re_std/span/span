/***********************************************************************
* re_std                                                            span
*
* umbrella header for <span>:
*   Pulls in every granular re_std::span module. Include this for the full
* std::span-equivalent surface, or include the individual granular
* headers under re_std/span/ to minimise translation-unit cost.
*
*   Contents:
*     re_std/span/dynamic_extent.hpp     - dynamic_extent constant
*     re_std/span/span.hpp               - span<T, Extent> + deduction guides
*     re_std/span/as_bytes.hpp           - as_bytes            (C++17+)
*     re_std/span/as_writable_bytes.hpp  - as_writable_bytes   (C++17+)
*
*   Deferred (pending re_std::ranges, not yet shipped):
*     - the contiguous-range constructor span(R&&)
*     - the ranges::enable_borrowed_range<span> / enable_view<span> opt-ins
*   These are tracked as depends_on "<ranges>" in the coverage data and
*   will land with that module.
*
*   Not a dependency: <string_view>. span has no relationship to
*   basic_string_view; the two modules are independent.
*
*
* path:      /inc/re_std/span
* link(s):   TBA
* author(s): re_std contributors                       date: 2026.06.04
***********************************************************************/

#ifndef DJINTERP_RE_STD_SPAN_
#define DJINTERP_RE_STD_SPAN_ 1

#include "re_std/span/dynamic_extent.hpp"
#include "re_std/span/span.hpp"
#include "re_std/span/as_bytes.hpp"
#include "re_std/span/as_writable_bytes.hpp"

#endif  // DJINTERP_RE_STD_SPAN_
