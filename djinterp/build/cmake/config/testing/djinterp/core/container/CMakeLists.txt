# =============================================================================
# testing/djinterp/core/container/
#   Aggregate over every container test (array, list, map, tree, ...).
# =============================================================================

djinterp_add_test_subtree(all_containers)
